# Proposed Future Rules

Status: proposed only

These rules are not active until the operator approves them.

## Candidates

1. Require `npm run validate:current` before any handoff package is called ready.
2. Require Compound Extraction for all friction level 2 or 3 runs.
3. Require source freshness checks for research runs that include current market, company, financial, legal, or product facts.
4. Require a schema-backed YAML parser once the validator begins enforcing nested artifact content.

## Not Automatically Activated

Compound output may propose future rules, but it cannot modify canonical policy or external records without operator approval.
